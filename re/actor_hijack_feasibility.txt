ACTOR HIJACK FEASIBILITY — FoM-lite (Fallout4.exe 1.11.191)
============================================================
All RVAs off image base 0x140000000. Reports cited in brackets.


1) SPAWN N ACTORS  →  FEASIBLE, SYNCHRONOUS
PlaceAtMe @ RVA 0x1159C10 is inline (no queued functor). Allocates
via sub_1416579C0 (0x490 B), runs Actor ctor sub_140C57640, then
PlaceRefIntoWorld core sub_1402E4DF0 with on-stack NEW_REFR_DATA.
All ctor vtables = Actor::`vftable`.
  [placeatme_decomp.txt:3-182, hijack_primitives_decomp.txt:127-227]
3 calls from our thread. OR 0x4000 into the flags (trace shows
0x1000000 only) to mark TEMPORARY — else save bloat.


2) TELEPORT ACROSS CELLS  →  1-TICK LAG
MoveTo @ RVA 0x11588D0 is async — builds MoveToFunctor and pushes
to ScriptTask queue qword_1430E0208 via sub_1410EAE30.
  [setposition_report.txt:33-146, placeatme_decomp.txt:265-400]
Intra-cell: direct write to REFR+0xD0 works (POS_OFF).
Cross-cell: direct write is silent (no cell-grid notify) → actor
falls out of world. Use MoveTo → 1 tick lag. Hybrid: direct for
same cell, functor on cell change. Next RE milestone:
MoveToFunctor::OnRun.


3) SWAP APPEARANCE  →  YES, SYNC
  SetRace     RVA 0x10FB010 → sub_140CA4420 (28-line sync)
              [hijack_primitives_decomp.txt:354-401]
  SetOutfit   RVA 0x10FAE20 — writes TESNPC+0x2B0 (default) or
              +0x2B8 (sleep), dirty-flag via vtable+0x68
              [hijack_primitives_decomp.txt:578-597]
  CombatStyle RVA 0x1107760 → sub_140CCD400 (4-line)
  Weapon:     existing INV_ADD_FROM_WORLD_RVA=0x500430.
Race swap triggers full 3D rebuild (~30 ms) — once at join, never
per tick.


4) MUTE AI  →  YES
  StopCombat         RVA 0x10FBDB0 → sub_140DAF2D0 (sync)
  IgnoreFriendlyHits RVA 0x1157D10 (sub_140CE0330 flag flip)
  SetAlly            RVA 0x11415E0 — ties to player singleton
                     qword_1432D2260 via vtable+0x6B0
  EvaluatePackage    RVA 0x10F8610 → sub_140C690B0
  ToggleCombatAI     global at xref 0x2EF2670 — kills combat for
                     ALL actors (breaks SP, don't use).
Per-ghost: SetAlly(player) at spawn + clear AI package list +
EvalPackage.


5) NPC TEMPLATE
EditorIDs live in ESM data. Recommend LCharWorkshopNPC (FormID
0x0020593F — settler base, Sandbox-only AI). Template identity
is irrelevant because we immediately ChangeRace + SetOutfit.


ENGINEERING COST
=================
HIJACK : Spawn 2d + SetPos hybrid 2d + Race/Outfit/Weapon 2d +
         MuteAI 0.5d + Pool 1d + MoveTo-core RE 1d = ~8.5 d
CUSTOM : ArmorAddon 5d + AnimFeed 8d + Shadow 3d + Lighting 4d
         = ~20 d
Hijack ~2.5× faster AND gains shadows, decals, lighting, impact
FX, dialogue camera FOR FREE.


TOP RISKS
==========
1. MoveTo async lag on cell-cross (1 tick) → hybrid direct+functor.
2. Cell-grid loads ~9 cells; far ghosts waste slots → Disable
   (RVA 0x11543B0) beyond N meters.
3. ToggleCombatAI is GLOBAL — breaks SP. Use per-actor SetAlly.
4. Race-swap 3D rebuild: only at join, never per-tick.
5. Save bloat: NEW_REFR_DATA flags MUST include 0x4000 TEMPORARY
   (trace had only 0x1000000).
6. Thread safety: hold game lock or push via ScriptTask queue
   qword_1430E0208 from D3D11 hook thread.


IDA SCRIPTS (this session)
===========================
  re/find_actor_hijack.py    — scans Papyrus strings, resolves
                               natives via lea-walk.
  re/decomp_placeatme.py     — decompiles PlaceAtMe/PlaceActorAtMe/
                               MoveTo/Disable/Delete natives.
  re/decomp_disable_core.py  — decompiles REFR ctor,
                               PlaceRefIntoWorld, ChangeRace,
                               StopCombat, EvalPackage, Outfit,
                               CombatStyle.
Reports: actor_hijack_raw.txt (399 L), placeatme_decomp.txt
(626 L), hijack_primitives_decomp.txt (600 L).


RECOMMENDATION
===============
GO HIJACK.

Every primitive is native-callable; ~80 % synchronous. 8.5 d vs
20+ d for renderer; you inherit shadows/lighting/decals/impact FX
free. Async MoveTo weakness is solvable via intra-cell direct
write (setpos_direct_report.txt).

Keep the D3D11 renderer as fallback for menus/disconnects/load
screens — safety net, not main loop.

Next RE target: MoveToFunctor::OnRun (vtable reachable from
sub_1411588D0) to remove cell-cross lag.

==== end ====
